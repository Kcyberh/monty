#include "monty.h"
/**
  *f_nop- it is nothing
  *@head: the stack head
  *@counter: the line_number
  *Return: it returns nothing
  */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
